[project]
name = "stream-mining-labs-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Wai", email = "waiwai00021@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.5",
]

[project.scripts]
stream-mining-labs-python = "stream_mining_labs_python:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
